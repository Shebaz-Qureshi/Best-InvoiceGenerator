---
import { guidesEs } from '../../../data/guides-es';

export function getStaticPaths() {
  return guidesEs.map((guide) => ({ params: { slug: guide.slug } }));
}

const { slug } = Astro.params;
return Astro.redirect(`/es/guides/${slug}/`, 301);
---
